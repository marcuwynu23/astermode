# Vite React TypeScript Example

This example shows how to use `AsterMode` in a React + TypeScript app powered by Vite.

## Run locally

From this folder:

```bash
npm install
npm run dev
```

Open the URL printed by Vite and confirm:

- the red **Dev Mode** badge appears in the bottom-left
- clicking it opens a context menu
- enabling hover border shows element dimensions
